Rlease check-list 
-----------------

* Change the release version in 
    ./CHANGELOG,
    ./eazytext/__init__.py
* Create a tag and push the tagged branch to launch-pad.
* Update setup.py and MANIFEST.in for release
* Upload the source into pypi.
        make upload
