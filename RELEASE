Rlease check-list 
-----------------

* Change the release version in 
    ./setup.py,
    ./CHANGELOG,
    ./zwiki/__init__.py
    ../zeta/bin/regrupgrade.py
    ./README

* Upload the source into launchpad.
* Rename the package to zwiki_zeta under pypi.
